apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: notifications-service-data
spec:
  accessModes:
  - ReadWriteOnce
  - ReadOnlyMany 
  resources:
    requests:
      storage: 10Mi

---

apiVersion: v1
kind: Service
metadata:
  name: notifications-service
spec:
  type: LoadBalancer
  ports:
  - port: 5008
    targetPort: 80
  selector:
    web-service: notifications

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: notifications-service
spec:
  selector:
    matchLabels:
      web-service: notifications
  template:
    metadata:
      labels:
        web-service: notifications
        system: server
    spec:
      affinity:
        podAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchLabels:
                web-service: notifications
            topologyKey: "kubernetes.io/hostname"
      containers:
      - name: notifications-service
        image: stefantestdocker/tasktronic-notifications-service:1.0
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        resources:
          limits:
            memory: "256Mi"
            cpu: "125m"
        env:
        - name: ApplicationSettings__Secret
          valueFrom:
            secretKeyRef:
              name: environment-secrets
              key: security-token-secret
        - name: NotificationSettings__AllowedOrigins
          valueFrom:
            configMapKeyRef:
              name: environment-settings
              key: notifications-allowed-origins
        volumeMounts:
        - name: notifications-service-data
          mountPath: /root/.aspnet/DataProtection-Keys
      volumes:
      - name: notifications-service-data
        persistentVolumeClaim:
          claimName: notifications-service-data